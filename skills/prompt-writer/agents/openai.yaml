interface:
  display_name: "Prompt Writer"
  short_description: "Write copy-ready staged prompts with handoff context."
  default_prompt: "Use $prompt-writer to turn my planning docs and handoff notes into a paste-safe background prompt plus staged execution prompts."
