interface:
  display_name: "PPT Creator Pan"
  short_description: "把文档稳妥做成高质量可编辑PPT，并用OfficeCLI做渲染QA"
  default_prompt: "Use $ppt-createrskill-pan to turn my source materials into a high-quality, mostly editable PPT/PPTX, then use OfficeCLI render/structure QA for targeted fixes."

policy:
  allow_implicit_invocation: true
